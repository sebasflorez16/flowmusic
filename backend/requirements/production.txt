# Dependencias exclusivas del entorno de producción.
-r base.txt

# Servidor WSGI/ASGI de producción.
# `uvicorn[standard]` incluye websockets (necesario para Channels/WebSockets).
gunicorn>=22.0,<23.0
uvicorn[standard]>=0.30,<1.0

# Monitoreo de errores
sentry-sdk[django]>=2.10
